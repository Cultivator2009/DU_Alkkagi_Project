# 세션 인계 노트 (최종 갱신 2026-09-26, Mac mini M4)

Claude Code로 진행한 작업의 인계용 요약입니다. 새 머신이나 새 세션에서 이 파일을 먼저 읽으면 맥락을 빠르게 따라잡을 수 있습니다. 설계 문서가 따로 없으므로 이 파일이 사실상 유일한 설계 문서입니다.

## 프로젝트 개요

한국 전통 알까기를 모티브로 한 3D 물리 기반 1:1 대전 게임입니다. 2023년 초기 프로토타입에서 중단되었다가 2026-07에 재개했습니다. Steam 출시가 목표입니다.

## 환경

- **Unity 6000.4.6f1** (Apple Silicon 네이티브). 원래 2022.3.1f1로 시작했고, 도중에 자동 마이그레이션된 뒤 Unity 6을 유지하기로 했습니다. 씬 3개는 `320ae1a`에서 Unity 6 포맷으로 다시 저장했습니다.
- 주 개발 머신은 **Mac mini M4**입니다. 경로에 공백이 없어야 합니다 (`~/UnityProject/DU_Alkkagi_Project`). 기존 **Windows PC**는 P2P 테스트용 두 번째 머신입니다.
- 원격 저장소: `https://github.com/Cultivator2009/DU_Alkkagi_Project.git`, 브랜치 `main`
- `steam_appid.txt`는 gitignore 대상이라 머신마다 루트에 직접 만들어야 합니다 (`480` 한 줄, `steam_appid.txt.example` 참고).

### 버전 관리에서 주의할 점 (과거에 실제로 사고가 났던 부분)

- `.gitignore`는 toptal VisualStudio 템플릿에서 왔습니다. 그 템플릿의 규칙이 Unity 파일을 두 번 삼킨 적이 있습니다. `*.meta`는 `c8cb3b8`에서 고쳤고, `**/[Pp]ackages/*`는 `f385e50`에서 고쳤습니다. 규칙을 추가할 때는 `git check-ignore -v <경로>`로 확인하세요.
- `Packages/manifest.json`이 추적되지 않던 시절에 **ugui와 Cinemachine이 조용히 빠졌습니다.** ugui는 컴파일 에러로 드러났지만, Cinemachine은 에러 없이 카메라만 고정되었습니다. Cinemachine은 **반드시 2.x(현재 2.10.7)**를 유지해야 합니다. 씬의 컴포넌트 GUID가 2.x 클래스를 가리키고, 3.x에서는 그 클래스들이 Deprecated로 밀려납니다.

## 게임 구조 요약

- `GameManager`는 MainMenuScene에서 생성되는 `DontDestroyOnLoad` 싱글톤입니다. GameScene을 떠날 때는 **반드시 `EndMatch()`를 호출**해야 합니다. 호출하지 않으면 다음 판이 이전 판의 플레이어, 돌, TurnController를 물려받습니다.
- **Player 0 = 흑 = 화면 아래 = 선공**, Player 1 = 백입니다(3~4인이면 2 = 청, 3 = 적). 진영 판정은 `PlayersManager.color`가 아니라 돌의 `playerIndex`로 합니다 (color는 어디서도 설정되지 않습니다).
- 플레이 테스트는 **MainMenuScene에서 시작**해야 합니다. GameScene에서 바로 Play하면 `SceneMgmt`가 `GameReadyProcess`로 설정한 직후 `GameManager.Start()`가 `Mainmenu`로 덮어써서 매치가 시작되지 않습니다 (기존 동작, 미수정).
- **알은 씬에 직접 배치하지 않고 매치 시작 때 생성합니다** (`6125930`, `a6dee60`).
  - `Board_GO`의 `BoardSetup`이 대전 규칙에 맞는 판(`BoardVariant`: `GoBoard` / `JanggiBoard`)을 켭니다.
  - `PieceTemplates` 아래의 비활성 템플릿을 복제합니다: 바둑알 흑/백, 장기말 1개.
  - `pieceID`는 흑·초가 `A`부터, 백·한이 `a`부터, 청이 `M`부터, 적이 `m`부터입니다. 네트워크 메시지가 이 ID로 알을 찾으므로 두 머신이 같은 규칙으로 생성해야 합니다. 개수가 다르면 게스트가 에러 로그를 남깁니다.
  - 알마다 크기(`GamePieceManager.radius`)가 있어서, 배치 간격, 구역, 조준 가이드가 모두 이 값을 씁니다.
- **양쪽 이름과 색**: 바둑알이면 흑/백, 장기말이면 초(녹색, 선공)/한(빨강)입니다. `SideStyle`이 정하고, UI 아이콘과 이름에 붙은 `SideMark`가 알 종류에 맞춰 다시 칠합니다.
- **온라인 턴 흐름** (`31f7a49`): 물리 시뮬레이션과 `TurnController`는 호스트만 돌립니다. 게스트의 튕기기(`FlickCommand`)는 `TurnController.TryApplyExternalFlick`으로 **일반 턴과 같은 흐름**을 탑니다. 턴이 끝날 때마다 `TurnResult`가 **남은 돌 전체의 최종 위치**를 담아 보내므로, 게스트 화면은 매 턴 호스트와 일치하게 수렴합니다. 게스트 돌은 kinematic 상태에서 `MovePosition`으로 움직입니다.
- **종료 규칙**: 한쪽 돌이 모두 나가면 녹아웃. 양쪽 마지막 돌이 같은 샷에 나가면 대전 규칙에 따라 쏜 쪽 패배(기본) / 쏜 쪽 승리 / 무승부입니다. 무승부는 승자 `null`(네트워크에서는 `-1`)로 표현합니다. 온라인에서 상대가 나가거나 연결이 끊기면 남은 쪽 **기권승**입니다. 사유는 `MatchEndReason`으로 표현합니다.
- 연속 전적은 `MatchSeries`가 셉니다 (온라인은 로비별, 로컬은 메인 메뉴에서 새로 시작할 때까지, 무승부 포함).
- **온라인 시작 순서**: 호스트는 게스트의 `ClientReady`를 받은 뒤에야 배치 단계나 첫 턴을 시작합니다. 전에는 게스트가 로딩 중일 때 호스트가 먼저 쏠 수 있었습니다.
- 튕기기 힘은 `당긴 거리 / maxDragDistance(1.0)` × `maxForce(50)`입니다. 3% 미만으로 놓으면 튕기지 않고 취소됩니다.
  - 이 값은 질량 3(바둑알)이 받는 충격량이고, 알은 `속도 = 힘 / 3 × (3 / 질량)^0.25`로 출발합니다 (`referenceMass`, `massExponent`, `a849f7f`). 바둑알은 예전과 똑같습니다.
  - 전에는 모든 알에 같은 충격량을 줘서(지수 1) 무거운 궁이 느리게 출발해 거의 못 밀었습니다. 0.25는 손가락으로 튕기는 것과 비슷해서, 무거운 알이 조금 느리게 나가는 대신 더 세게 칩니다.
  - 측정(35% 세기, 3cm 간격에서 졸을 칠 때 졸이 밀린 거리): 예전에는 졸→졸 0.76, 궁→졸 0.20이었고, 지금은 0.56과 0.72입니다. 졸이 궁을 칠 때는 0.17로, 궁은 여전히 가장 안 밀립니다.
- **위로 뜨는 속도 제한** (`maxRiseSpeed` 0.4 m/s, 모든 알, 권위 쪽만): 튕기는 힘은 항상 수평이지만, 기울어진 알(겹친 알, 경첩 위)은 받치고 있는 것에 부딪혀 위로 튕깁니다. 제한 전에는 겹친 알을 받친 쪽으로 쏘면 판 위 2.0까지 날아갔고, 지금은 0.2 안쪽입니다. 평평한 샷은 영향이 없습니다.
- **프로토콜 버전** (`NetMessage.ProtocolVersion`, 현재 5): 메시지 형식을 바꾸면 **반드시 올리세요.** 로비가 이 값을 광고하고, 다른 버전의 로비는 목록에 나오지 않으며 코드로 들어가도 거절됩니다("게임 버전이 달라 참가할 수 없어요").
  - 1 → 2: 소리 이벤트(`BoardSound`), 기권(`Concede`, `MatchEndReason.Surrender`)
  - 2 → 3: 소리 이벤트에 위치
  - 3 → 4: 4인 대전(`LoadGameScene`에 명단, `PlayerOut`, 규칙 id 재배열)
  - 4 → 5: 명단에 자리별 레이팅, 규칙 "모드"(맨 앞, id 재배열), 판·알의 "랜덤"

## 대전 규칙 (`MatchSettings`, 2026-09-23)

규칙은 모두 로비 단위입니다. 온라인은 호스트가 로비에서 정하고, 로컬은 "로컬 대전"을 누르면 뜨는 대전 설정 카드에서 정합니다. 승부에 영향이 없는 개인 설정은 아래 "설정 화면과 조작"에 있습니다.

| 규칙 | 값 | 기본값 |
|---|---|---|
| 모드 (온라인만) | 일반전 / 커스텀 (아이템전은 나중에) | 일반전 |
| 판 | 바둑판 / 장기판 (경첩) / 랜덤 | 바둑판 |
| 알 | 바둑알 / 장기말 / 랜덤 | 바둑알 |
| 조준 가이드 | 켜기 / 끄기 | 켜기 |
| 인원 (온라인만) | 최대 2 / 3 / 4명 | 2명 |
| 흑·초 / 백·한 알 개수 | 각각 1–12 (비대칭 가능) | 6 / 6 |
| 청 / 적·흑 알 개수 (온라인, 3·4명일 때) | 각각 1–12 | 6 / 6 |
| 돌 배치 | 기본 배치 / 직접 배치 | 기본 배치 |
| 배치 방식 (직접 배치일 때) | 동시·비공개 / 동시·공개 / 번갈아 한 개씩 | 동시·비공개 |
| 배치 시간 (직접 배치일 때) | 30 / 45 / 60 / 90 / 120초 | 60초 |
| 턴 제한시간 | 제한 없음 / 10 / 15 / 20 / 30 / 60초 | 제한 없음 |
| 동시 소진 시 | 쏜 쪽 패배 / 쏜 쪽 승리 / 무승부 | 쏜 쪽 패배 |

- **일반전에서 고정되는 규칙** (`MatchSettingDef.RankedValues`): 알 개수 6개씩, 기본 배치, 동시 소진 시 쏜 쪽 패배. 턴 제한시간은 10–60초 중에서만 고를 수 있고 기본 30초입니다("제한 없음" 불가). 나머지(판, 알, 조준 가이드, 인원)는 호스트가 고릅니다. 아래 "모드" 참고.
- **랜덤**: 판과 알의 "랜덤"은 매치를 시작할 때 호스트가 굴려(`MatchSettings.Resolve`) 결과를 보냅니다. 재대결도 새로 굴립니다. 굴리기 전 선택은 `MatchSettings.Picked`에, 실제 매치 규칙은 `MatchSettings.Current`에 있습니다. 로컬도 같습니다.
- **규칙 추가 방법**: `MatchSettingId`에 항목 추가 → `MatchSettings.Defs`에 정의 한 줄(키, 라벨, 허용값, 기본값, 표시 형식, 필요하면 적용 조건) → 타입 있는 접근자 → `Loc`에 문자열 → `Tools > Alkkagi UI`의 1, 3, 4번을 다시 실행합니다. 로비 데이터(`rule.<key>`), PlayerPrefs(`match.<key>`), 네트워크(`LoadGameScene`)는 자동으로 따라옵니다.
- **적용 시점**: 호스트가 매치를 시작할 때 규칙 전체를 `LoadGameScene`에 실어 보냅니다. 재대결도 같습니다. 게임 코드는 `MatchSettings.Current`만 읽습니다.
- **사전 배치**: `Board_GO/SpawnLayouts/<개수>/` 아래 점들이 흑의 배치입니다. Scene 뷰에서 끌어서 조정하면 되고, 백은 판 중심을 기준으로 180° 돌려서 씁니다. `Tools > Alkkagi > Reset spawn layouts`로 기본값을 다시 만들 수 있습니다 (7개까지 한 줄, 8개부터 두 줄, 6개는 예전 배치와 같음).
- **직접 배치** (`PlacementPhase` 규칙, `PlacementController` 입력/화면, `PlacementHud` 카드): 자기 진영(`BoardSetup.blackZone`, 백은 대칭)을 클릭해 놓고, 동시 방식에서는 준비 완료 전까지 끌어서 옮길 수 있습니다. 각자 시계가 있고, 0이 되면 남은 돌을 진영 안에 무작위로(겹치지 않게) 놓습니다. 로컬 핫시트는 동시 방식도 흑 → 백 순서로 진행합니다. 온라인은 호스트가 규칙을 판정합니다. 게스트는 자기 사본에 먼저 놓아 보이고, 호스트의 `PlacementState`가 확정하거나 되돌립니다.
- **장기판** (`Tools > Alkkagi > Build boards and janggi pieces`로 다시 생성): 판은 2.8×3.0이고, 9×10 줄과 궁성 2개를 그린 생성 텍스처를 씁니다. 접는 선(z=0) 위 x=±0.8에 **경첩 2개**가 있습니다.
  - 경첩은 판 위로 0.010 솟아 있습니다. 충돌체는 겉모습보다 넓은 캡슐을 판 속에 묻은 형태라 완만한 턱으로 작동합니다. 위로 튀는 속도는 알 쪽의 `maxRiseSpeed`가 막습니다(예전 `HingeBump`는 이것으로 대체).
  - 2026-09-24 재검토(높이 0.006–0.024, 알 4종, 세기 5단계 실측)에서 **0.010을 유지**하기로 했습니다:
    - 장기말: 졸·차는 40%, 궁은 70% 이상이어야 넘고, 넘으면서 속도가 절반쯤 줄어듭니다. 0.006이면 경첩이 거의 의미가 없고, 0.014부터는 최대 세기도 판 가운데에서 멈추며, 0.024는 벽입니다.
    - 바둑알: 높이와 상관없이 40–55% 이상이면 모서리가 걸려 뒤집히며 0.07 튀어 오릅니다(스키 점프). 바둑알 충돌체가 납작하고 모서리가 각져서 생기는 현상이라 높이로는 바뀌지 않습니다.
  - 조정 값은 빌더의 `proud`, `bumpRadius`입니다.
- **장기말**: `JanggiPieceMesh`가 코드로 만드는 팔각형입니다. 위아래 모따기가 있고, 크기는 궁 / 차·포·마·상 / 사·졸·병 3가지입니다.
  - 글자는 주인 쪽을 향합니다. 한글/한자는 각자 설정에서 고릅니다(아래 "설정 화면과 조작").
  - 질량은 부피에 비례합니다(바둑알 3 기준으로 궁 6.3, 차 4.1, 졸 2.5).
  - 개수 N이면 `BoardSetup.janggiLineup`의 앞 N개를 씁니다. 뒷줄부터, 각 줄은 가운데부터 채워서 궁이 뒷줄 가운데에 옵니다.
- **턴 제한시간/스킵**: 시간이 다 되면 턴을 넘깁니다. 자기 차례에는 패널 옆 "턴 넘기기" 버튼으로 직접 넘길 수 있습니다. `TurnController.OnTurnPassed`와 `TurnResult`의 `TurnEnd`(Shot / Timeout / Skipped)로 구분하고, 넘긴 턴은 튕긴 횟수에 세지 않습니다.
  - **마우스를 놓는 순간 샷이 확정됩니다** (`TurnController.ShotReleased`): 그때부터 시계가 멈추고, 턴은 알이 멈춘 뒤 넘어갑니다. 전에는 놓은 직후나 샷이 적용된 바로 그 프레임에 시간이 0이 되면 "시간 초과"로 넘어갔습니다. 그러면 알이 상대 턴에 날아가고, 샷 처리 중일 때만 보내는 스냅샷이 나가지 않아 게스트 화면에는 반영되지 않았습니다(세 번째 테스트에서 발견).
  - **게스트 턴의 유예**: 게스트의 샷은 네트워크를 건너 호스트에 도착하므로, 호스트는 게스트 턴을 0에서 0.5초 더 기다린 뒤 넘깁니다 (`RemoteGraceSeconds`, `NetworkMatchBridge.GuestFlickGraceSeconds`). 그 사이 호스트 화면의 시계는 0으로 보입니다.

## 킬로그와 결과판 (2026-09-24)

- 알이 떨어지면 그 샷을 쏜 사람의 기록이 됩니다 (`KillLog`, 샷이 멈춘 뒤 판정):
  - **킬**: 상대 알이 떨어짐
  - **논개**: 쏜 알이 상대 알과 함께 떨어짐. 킬 줄에 "논개" 배지가 붙고, 쏜 알은 따로 줄을 만들지 않습니다. 논개 횟수는 샷 단위로 셉니다(한 번에 둘을 데려가도 1).
  - **자살**: 쏜 알이 상대 알을 데려가지 못하고 혼자 떨어짐
  - **팀킬**: 쏜 샷에 자기 편 다른 알이 떨어짐
- 킬피드(`KillFeed`)는 CS2처럼 화면 왼쪽 위에 줄마다 "쏜 쪽 · 쏜 알 ▶ 떨어진 알 · 그 편"을 보여 주고, 6초 뒤 사라집니다. 장기말은 아이콘에 글자가 들어갑니다. 온라인에서는 내가 쏜 줄의 테두리가 빨갛습니다.
- 결과판 행: 남은 알 / 킬 / 논개 / 자살 / 팀킬 / 튕긴 횟수(랭크 매치면 레이팅 줄이 더 붙음). 옆 패널의 "잡은 알"은 점수라서 상대의 자살·팀킬도 포함합니다.
  - 레이팅 줄이 없는 판에서는 결과판과 결과 창이 한 줄만큼 줄어듭니다(`MainGameUIController.ShowRatingRow`). 전에는 그 자리가 빈칸으로 남았습니다.
- 온라인은 호스트가 판정하고 `TurnResult`에 이벤트를 실어 보냅니다. 게스트의 `KillLog.Record`가 같은 이벤트를 받아 피드와 결과판에 씁니다.

## 공개 로비 (2026-09-24)

- **공개 범위**(호스트가 로비에서 선택, 다음 로비의 기본값으로 저장):
  - 공개: 목록과 빠른 대전에 나오고 친구도 봅니다
  - 친구: Steam 친구 목록에서 참가하거나 코드로 참가합니다. 예전 동작과 같고 기본값입니다
  - 비공개: 코드나 초대로만
- **공개 로비 목록**: 로비 화면 오른쪽 카드입니다. 들어올 때, 로비를 나갈 때, "새로고침"을 누를 때 검색합니다. 호스트 이름, 판·알·알 개수, 참가 버튼이 있습니다.
- **빠른 대전**: 가장 가까운 빈 공개 로비에 들어가고(찼으면 다음 것), 없으면 공개 로비를 만들어 기다립니다.
- **로비 데이터**: `game=du-alkkagi`(480 테스트 앱은 모든 개발자가 같이 써서 이 값으로 거릅니다), `proto`, `state`(open/playing), `host`(목록용 이름), `vis`. Steam에는 로비 종류를 읽는 API가 없어서 `vis`에 따로 적습니다.
- **대전 중에는 목록에서 빠집니다**: 시작할 때 `SetJoinable(false)` + `state=playing`, 로비 화면으로 돌아오면 다시 엽니다.
- **내보내기**: Steam 로비에는 강퇴 기능이 없어서, 호스트가 게스트에게 `Kick` 메시지를 보내면 게스트 쪽 게임이 로비를 나갑니다. 같은 로비에 다시 들어오면 또 내보냅니다. 조작된 클라이언트는 무시할 수 있습니다.
- 에디터 한 대 + 실제 Steam으로 확인한 것:
  - 공개 로비를 만들면 검색에 나오고, 친구 공개로 바꾸거나 대전 중으로 바꾸면 빠지고, 다시 열면 나옵니다
  - 빈 로비가 없을 때 빠른 대전은 공개 로비를 만들어 기다립니다
- **정식 출시에는 자체 App ID가 필요합니다** (Steamworks 등록비 $100). 480에서는 다른 게임의 로비가 섞여서 `game` 키로 거르고 있을 뿐입니다.

## 사운드 (2026-09-24)

- 소리는 `Tools > Alkkagi > Build sounds`가 코드로 합성한 WAV 18개입니다 (`Assets/Audio/`, `Assets/Resources/SoundBank.asset`).
  - 판 위: 바둑알 충돌, 장기말 충돌, 경첩, 튕기기, 낙하, 알 놓기(배치)
  - 인터페이스: 클릭, 초읽기, 턴 시작, 킬, 승리·패배·무승부, 대전 시작(박), 조준 세기 눈금, 조준 취소, 결과 도장, 창 열기(종이)
  - 녹음한 소리로 바꾸려면 같은 이름으로 WAV를 덮어쓰고, 빌더는 다시 돌리지 마세요.
  - 2026-09-26에 추가한 6개는 빌더에서 기존 12개 뒤에 만들어서, 다시 돌려도 기존 12개는 바이트까지 같습니다(노이즈 난수 순서가 그대로). 새 소리도 맨 뒤에 추가하세요.
- **언제 나는지** (2026-09-26 추가분):
  - 대전 시작: 첫 턴이 시작될 때 "박"(궁중음악을 여는 나무 딱따기) 소리와 "대전 시작 · 흑 먼저" 알림
  - 조준 세기 눈금: 당기는 동안 10%마다 작은 틱, 세질수록 높게(래칫 느낌)
  - 조준 취소: 오른쪽 클릭, 너무 약하게 놓음, 시점 이동으로 조준이 풀릴 때
  - 알 놓기: 배치 단계에서 이 화면이 알을 놓거나 옮길 때(상대의 비공개 배치는 소리로도 알 수 없음)
  - 결과 도장: 결과 창의 도장이 내려앉을 때
  - 창 열기: 설정, 대전 설정, 랭킹, 게임 중 메뉴가 열릴 때(결과 창은 도장 소리로 대신)
  - 로비: 누가 들어오면 턴 시작 종, 나가면 취소 소리. 다른 창을 보며 기다리는 호스트용입니다
- 재생은 `GameAudio`(소스 12개를 돌려 씀)가 합니다. 판 위 소리는 전체 음량만, 인터페이스 소리는 인터페이스 음량도 곱합니다. 판 위 소리는 x 위치에 따라 좌우로 나뉩니다.
- **판 위 소리는 물리 권위 쪽에서만 납니다** (`PieceSounds`의 충돌 이벤트, 한 물리 프레임에 최대 4개).
  - 게스트의 알은 kinematic이라 충돌이 일어나지 않습니다. 그래서 호스트가 소리마다 이벤트(`NetMessage.BoardSound`, unreliable, 종류·세기·위치 15바이트)를 보내고, 게스트는 화면이 스냅샷을 따라가는 지연만큼(0.06초) 늦게 재생합니다. 좌우 위치는 받은 쪽이 위치로 계산합니다.
  - 게스트 자신의 튕기기는 놓는 순간 바로 나고, 호스트는 그 튕기기를 다시 보내지 않습니다.
  - 알 움직임으로 소리를 감지하는 방식은 시험해 보고 버렸습니다. 한 프레임 늦게 반응해 호스트에서도 충돌을 놓쳤고, 게스트는 부드럽게 따라가느라 궤적이 뭉개졌습니다.
- 빠른 샷이 연속 충돌 처리로 한 프레임 안에 잡히면 Unity가 충돌 속도를 0으로 줍니다. 그래서 충돌음 세기는 "충돌 속도와 두 알이 갈라지는 속도 중 큰 값"으로 정합니다.
- UI 빌더가 만드는 모든 버튼에는 `ClickSound`가 붙습니다.

## 게임 중 메뉴 (2026-09-24)

- Esc나 오른쪽 아래 "메뉴" 버튼으로 엽니다 (`PauseMenu`). 계속하기 / 설정(메인 메뉴와 같은 카드) / 기권 / 메인 메뉴로.
- 기권과 메인 메뉴는 3초 안에 한 번 더 눌러야 확정됩니다.
- 로컬(2인, AI)은 메뉴가 열려 있는 동안 `Time.timeScale = 0`으로 멈춥니다. 물리, 턴 시계, 배치 시계, AI 생각 시간이 모두 멈춥니다. 닫으면 게임 속도(`GamePace.Speed`)로 돌아갑니다. 온라인은 멈추지 않는다고 표시합니다.
- **기권**: 턴이 시작된 뒤에만 가능하고(배치 중 불가), 결과는 `MatchEndReason.Surrender`입니다.
  - 2인 로컬: 지금 차례인 쪽이 기권합니다(버튼에 "흑 기권"처럼 표시).
  - 온라인: 호스트가 판정합니다. 게스트는 `Concede` 메시지를 보냅니다.
- 메뉴를 열면 진행 중인 조준을 취소합니다. UI 위를 클릭해도 뒤에 있는 알이 선택되지 않게 `OnMouseDown`에서 막습니다(전에는 모든 UI에서 그랬습니다).
- 받침 없는 "초" 뒤에 "이"가 붙던 문장("쏜 초이 졌어요")을 "쏜 초 쪽이 졌어요"로 고쳤습니다.

## AI 상대 (2026-09-24)

- 로컬 대전 설정 카드 맨 위의 "상대: 2인 / AI 쉬움 / AI 보통 / AI 어려움"에서 고르고, 선택은 PlayerPrefs(`local.opponent`)에 저장됩니다. AI는 백(한), 사람은 흑(초)이고, 사람이 먼저 쏩니다.
- **샷 고르기** (`AIPlanner`):
  - AI 차례마다 따로 된 물리 씬에 판 충돌체와 알 복제본을 만듭니다(스크립트 없음, 렌더링 끔). 후보 샷을 실제 게임과 같은 방식으로 스텝합니다(출발 속도, 위로 뜨는 속도 제한).
  - 후보: 내 알마다 상대 알 정면, 좌우로 비껴 치기 × 세기 3가지(떨어뜨리는 데 필요한 추정 세기, 1.25배, 풀파워). 우선순위가 높은 것부터 쉬움 12 / 보통 30 / 어려움 60개를 시험합니다.
  - 점수: 상대 알 +10, 내 알 −12, 승리 +1000(동시 소진은 대전 규칙을 따름). 동점이면 상대 알은 가장자리 쪽, 내 알은 가운데 쪽을 선호합니다.
  - 한 프레임에 6ms씩만 계산해서 게임이 끊기지 않습니다. 한 턴 계획은 0.05–0.5초입니다.
- 오차 없이 쏘면 예측이 실제와 10턴 모두 일치했습니다. 그래서 **난이도는 실제로 쏠 때의 오차로 나눕니다** (`AIOpponent.Error`): 조준 ±5° / ±2.5° / ±1°, 세기 25% / 12% / 5%. 쉬움은 40% 확률로 2–3번째 후보를 고릅니다.
- 추정 세기 계산의 두 상수는 실측값입니다: 판 위 감속 약 17.5 m/s², 정면 충돌 시 속도 전달 약 70%(반발계수 0.4).
- 배치 규칙이면 AI도 자기 진영에 무작위로 놓습니다. 사람과 AI가 동시에 배치하고, 번갈아 방식이면 한 번에 하나씩 놓습니다.
- 결과 화면과 효과음은 사람 쪽 기준(승리/패배)이고, AI 쪽 패널에는 "AI 보통"처럼 표시됩니다. 연속 전적은 난이도별로 따로 셉니다.

## 게임 속도, 시점 이동, 충돌 이펙트 (2026-09-25)

- **게임 속도 0.7배** (`GamePace.Speed`, 2026-09-26에 0.8에서 낮춤): 매치 중에는 `Time.timeScale = 0.7`입니다.
  - 물리 스텝(0.02초)은 그대로이고 실제 시간으로 초당 35번 돌 뿐이라, 궤적·충돌·경첩 점프·AI 예측이 전과 똑같고 느리게만 보입니다. 같은 샷이 0.8배와 1배에서 똑같이 0.422m를 갔습니다. 알은 Rigidbody 보간(Extrapolate)이 켜져 있어 초당 35스텝이어도 화면에서는 매끄럽게 움직입니다.
  - 튕기는 힘을 줄이거나 마찰을 늘리는 방식은 속도와 함께 이동 거리도 바뀌므로 쓰지 않았습니다.
  - 턴 시계, 배치 시계, 게스트 쪽 시계는 실제 초로 흐릅니다(`GamePace.ClockDelta`, 일시정지 중에는 0). 결과판의 경기 시간은 `Time.time`을 속도로 나눠 실제 시간으로 보여 줍니다.
  - 메뉴와 로비는 1배입니다(`EndMatch`가 되돌림). 킬피드와 알림 표시 시간, 카메라 전환은 게임 시간이라 같이 느려집니다.
  - 값을 바꾸려면 `GamePace.Speed` 한 줄만 고치면 됩니다. 두 머신이 같은 값이어야 체감이 같습니다(판정은 호스트가 하므로 어긋나도 결과는 같습니다).
- **카메라** (`CameraRig`, 매치 시작 때 GameScene에 생성). 시점 전환 키 처리도 `GameManager`에서 여기로 옮겼습니다.
  - **시점 이동** (`GameAction.PanView`, 기본 마우스 가운데 버튼): 누른 채 끌면 기본 시점 그대로 판을 잡아 수평으로 옮깁니다. 잡은 지점이 커서 밑에 계속 붙어 있습니다.
    - 화면 중심은 판 가운데에서 가장자리까지 거리의 절반(`reach`) 안에서만 움직입니다. 바둑판에서는 ±0.75이고, 판 대부분이 항상 보입니다.
    - 커서 밑 지점은 Main Camera가 아니라 메인 가상 카메라의 자세로 계산합니다. 시점 전환이 블렌드 중이어도 튀지 않습니다.
  - **확대·축소** (마우스 휠, 2026-09-27): 메인 시점이 판에 조금 다가가거나(0.94배 거리까지) 물러납니다(1.35배까지). 물러나면 같은 세기에 필요한 마우스 이동이 짧아져서, 전체 화면에서 화면 가장자리 가까운 알을 당길 때 커서가 가장자리에 걸리지 않습니다. 더 가까이 가면 판이 HUD 밑으로 들어가서 0.94배에서 멈춥니다. UI 위, 자유 시점, 일시정지 중에는 받지 않고, 조준 중에는 됩니다(당기다 모자라면 휠로 물러나 이어 당길 수 있음). 휠은 바인딩 대상이 아니고, 조작 안내에 "휠 / 확대·축소" 줄이 있습니다.
  - **시점 초기화** (`GameAction.ResetView`, 기본 R, 또는 옮기거나 확대·축소했을 때만 메뉴 버튼 왼쪽에 보이는 HUD 버튼): 이동과 확대·축소 모두 원래대로 미끄러지듯 돌아갑니다.
  - **자유 시점(Ctrl)은 옮긴 중심을 기준으로 돕니다.** FreeLook의 대상(`CameraAxis`)을 화면 중심과 같이 옮깁니다.
  - **카메라가 움직이는 동안(시점 이동 중, 자유 시점 중)은 조준과 배치를 받지 않습니다** (`CameraRig.Busy`). 시작하는 순간 진행 중이던 조준은 취소되고, 끌던 배치 돌은 그 자리에 놓입니다.
  - 메인 가상 카메라는 태그 `vcam` 중 우선순위가 가장 높은 일반 가상 카메라로 찾습니다. 이전 코드처럼 `vcams[0]` 순서에 기대지 않습니다.
- **충돌 이펙트** (`HitEffects`, `Assets/Shaders/HitEffect.shader`, `Assets/Resources/HitEffect.mat`), 2026-09-27에 다시 만듦:
  - **모든 효과는 판 표면에 평평하게, 알 밑에 그립니다** (셰이더 `ZTest LEqual`, 판 위 0.002). 알(불투명)이 먼저 그려져 깊이를 쓰므로 효과가 알을 가릴 수 없습니다. 전에는 깊이를 무시하고 맨 위에 그리는 노란 별(반지름 최대 0.2, 최대 0.75초)이 바로 그 샷에 움직이는 알들을 자주 가렸습니다(사용자 피드백).
  - 알끼리 부딪히면: 부딪힌 곳에서 판 위로 먹 선 4–8개가 뻗고(0.1–0.3초), 고리가 퍼지고(0.14–0.4초), 그 밑에 하얀 빛이 아주 잠깐 번집니다(0.06–0.16초, 가운데서 가장자리로 투명해짐). 아주 약한 충돌(세기 0.1 미만, 약 1.2 m/s)은 고리만 납니다.
  - 세기(충돌 속도에 비례, 12 m/s가 최대)가 크기(0.05–0.16)와 선 개수, 지속 시간을 정합니다. 가장 긴 것도 0.4초(게임 시간)입니다.
  - 셰이더의 스텐실은 그대로입니다: 픽셀마다 처음 그린 색만 남고, 앞에서 뒤 순서(먹 선 → 고리 → 빛, 최신 충돌 먼저)로 그립니다.
  - 한 번에 최대 48개입니다.
  - 충돌음이 날 때 같이 나므로(`BoardSounds.Play`), 게스트도 소리와 같은 때와 곳에 봅니다. 한 물리 프레임에 4개 제한도 소리와 같습니다.
  - 모든 효과를 메시 하나로 매 프레임 다시 만듭니다. 게임 시간을 따르므로 일시정지하면 멈춥니다.
  - 경첩 충돌에는 효과가 없습니다.
- **다른 판 위 효과** (2026-09-26, 같은 `HitEffects`의 `Kind`):
  - **낙하(킬의 순간)**: 알이 판 가장자리를 넘어 떨어지는 순간, 넘어간 가장자리에 먹 자국이 번지고 먹방울 7개가 판 바깥쪽 부채꼴로 튀며, 붉은(낙관색) 고리가 퍼집니다(0.65초). 결과판·킬피드는 샷이 멈춘 뒤 나오므로, 이것이 바로 보이는 첫 신호입니다.
  - **튕기기**: 튕긴 알 둘레에 얇은 먹 고리(0.3초). 셀수록 넓습니다.
  - **턴 시작**: 이 화면이 둘 차례가 되면 그 쪽 알마다 고리가 하나씩 차례로 퍼집니다(0.05초 간격, 0.7초). 핫시트는 매 턴, 온라인·AI는 자기 턴만(턴 종소리와 같은 조건).
  - 낙하와 튕기기는 소리 이벤트(`BoardSounds.Play`)에서 나므로 게스트도 같은 때와 곳에 봅니다. 턴 고리는 HUD가 턴을 알릴 때 냅니다.
  - 새 효과(아이템 등)를 넣으려면 `HitEffects.Kind`에 항목, `PlayXxx`, `Draw` 분기와 수명(`Life`)을 추가하세요. 모두 메시 하나로 그려지고 스텐실 규칙(먼저 그린 것이 위)이 같이 적용됩니다.
- **마우스를 올린 알**: 이 화면이 지금 집을 수 있는 알 위에 커서가 있으면 알 둘레에 숨 쉬듯 커졌다 작아지는 먹 고리가 뜹니다(`AimIndicator.hoverRing`, 판단은 `MainGameUIController.MayPickUp`). 시점 이동·일시정지·UI 위에서는 뜨지 않습니다.
- **프로토콜 3**: `BoardSound` 메시지에 위치가 들어가 모양이 바뀌었습니다. 두 머신이 같은 커밋이어야 서로 로비가 보입니다.

## 레이팅과 랭킹 (2026-09-26)

- **모드** (로비 규칙 맨 앞, `MatchMode`):
  - **일반전**(기본): 레이팅에 반영됩니다. 밸런스에 영향이 큰 규칙은 고정됩니다(위 "대전 규칙"의 일반전 고정 규칙). 고정된 줄은 흐리게, 화살표 없이 보입니다.
  - **아이템전**: 레이팅에 반영할 예정입니다. enum 자리만 있고 선택지에는 아직 없습니다. 아이템 규칙이 생기면 `MatchSettings.Defs`의 모드 값 목록에 넣으세요.
  - **커스텀**: 모든 규칙을 바꿀 수 있고 레이팅에 반영하지 않습니다.
  - 커스텀에서 일반전으로 바꾸면 고정 규칙이 일반전 값으로 돌아갑니다(`ApplyMode`). 호스트의 규칙 저장(PlayerPrefs)은 일반전에서 고정 규칙을 빼고 저장해서, 로컬 설정에서 쓰던 값을 덮어쓰지 않습니다.
  - 반영 여부(`MatchSettings.Rated`)는 "일반전이고 일반전 규칙 안의 값인가"입니다. 규칙을 어긴 값을 보내는 호스트의 매치는 모든 머신에서 미반영이 됩니다.
  - 빠른 대전은 일반전 로비만 찾고, 없으면 일반전 로비를 만듭니다. 공개 로비 목록은 "일반전 · 바둑판 · 바둑알 · 2/4명"처럼 모드를 앞에 보여 줍니다.
  - 로컬 대전과 AI 대전은 모드 없이 모든 규칙을 쓸 수 있고, 레이팅에 반영하지 않습니다.
- **계산** (`Elo`, `RatedMatch`): 시작 1000, K=32인 Elo입니다.
  - 2인은 보통 Elo입니다(동급이면 ±16).
  - 3~4인은 상대마다 한 판씩 이긴 것/진 것/비긴 것으로 치고, K를 (인원-1)로 나눕니다. 그래서 동급 넷 중 1등도 +16입니다.
  - 순위는 탈락 순서입니다. 먼저 나간 쪽이 지고, 같은 샷에 함께 나간 쪽끼리는 비깁니다. 기권과 나감은 항상 따로 한 순위입니다.
  - 동급 4인 예: 1등 +16, 2등 +5, 3등 -5, 4등 -16.
  - 3~4인에서 호스트가 나가면 호스트는 남은 모두에게 진 것으로 칩니다. 남은 사람끼리는 반영하지 않고, 결과는 "무효"로 셉니다.
- **레이팅 숫자** (`MatchRoster`): 호스트가 로비 멤버 데이터(`rating`, 각자 자기 값을 올림)를 읽어 명단에 자리별 레이팅을 싣습니다. 모든 머신이 같은 숫자로 모두의 변화를 계산하고 결과판에 보여 줍니다. 자기 레이팅만은 자기 기록(`PlayerRating`)의 값을 씁니다.
- **중도 이탈** (`PlayerRating.Hold`): 매치가 시작되는 순간 "지금 나가면 받을 결과"(남은 모두에게 패배)를 PlayerPrefs에 저장해 둡니다.
  - 탈락해서 관전 중이면 이미 정해진 순위로 갱신합니다.
  - 결과가 나오면 실제 결과로 바꿔 반영합니다.
  - 결과 전에 나가면(메뉴, 앱 종료, 크래시, 연결 끊김) 다음에 메인 메뉴나 로비로 돌아오거나 게임을 켤 때 저장된 결과가 반영됩니다.
- **저장** (`PlayerPrefs` `rank.<SteamID>.*`): 레이팅, 판수, 승, 패, 무입니다. 결과가 나올 때마다 **Steam 리더보드**(`RankBoard`, 이름 `du-alkkagi-rating`)에 올립니다. 점수는 레이팅이고, 세부값은 [판수, 승, 패, 무]입니다. 항상 덮어써서(`ReplaceScore`) 레이팅이 내려가도 반영됩니다.
  - 게임을 켤 때 리더보드의 내 기록과 비교합니다. 판수가 더 많은 쪽을 믿습니다. 다른 기기에서 더 했으면 받아 오고, 못 올린 기록이 있으면 다시 올립니다.
  - 리더보드는 처음 찾는 게임이 만듭니다(`FindOrCreateLeaderboardAsync`). 480 테스트 앱은 모든 개발자가 같이 쓰므로 이름에 게임 이름을 붙였습니다. 2026-09-26 에디터 확인 중에 빈 보드가 생성되었고, 점수는 아직 올리지 않았습니다.
- **화면**
  - 메인 메뉴: "랭킹" 버튼(설정·랭킹·종료 세 개), 아래 Steam 줄에 내 레이팅
  - 랭킹 카드(`LeaderboardPanel`): 전체 / 내 주변 / 친구 탭, 10줄, 내 줄 강조, 위에 "내 레이팅 1104 · 17승 6패 · 4위"
  - 로비: 이름 옆 레이팅 칩. 아직 공유하지 않은 사람은 칩이 없습니다.
  - 결과판: "레이팅" 줄에 새 레이팅과 변화(초록 +, 빨강 -). 랭크 매치에서만 보입니다.
- **신뢰 문제**: 각자 자기 기록을 올리는 구조라, 조작한 클라이언트는 아무 값이나 올릴 수 있습니다. 매치 판정도 호스트를 믿는 구조입니다. 친구 테스트용으로는 충분하지만, 공개 랭크를 제대로 하려면 서버 검증(또는 Steam의 "신뢰된" 리더보드 쓰기를 서버에서 하는 방식)이 필요합니다.

## 화면 비율 (2026-09-26)

16:9, 16:10, 4:3, 21:9, 5:4를 에디터에서 렌더해 확인했습니다. 지금은 모두 잘리거나 겹치지 않습니다.

- **메뉴와 로비** (`UIKit.Canvas`의 `Expand`): 캔버스가 항상 1920×1080 이상이라, 설계한 배치가 늘 화면 안에 들어오고 남는 공간은 긴 쪽으로 갑니다.
  - 전에는 0.5 매칭이었습니다. 그래서 21:9에서 설정 창과 로컬 대전 설정 창의 위아래가, 5:4에서 로비 오른쪽 카드가 잘렸습니다.
  - 메인 메뉴의 로고와 버튼은 판 그림처럼 세로 가운데를 기준으로 걸어서, 4:3에서도 위로 몰리지 않습니다.
- **게임 HUD**: 높이 기준 그대로입니다. 판이 화면 높이를 채우므로(판 폭은 화면 높이의 0.86배), 16:10 이하에서는 판 옆 여백이 좁아집니다.
  - `MainGameUIController.Arrange`가 여백(`CameraRig.HomeHalfWidth`로 계산)에 맞춰 판 옆 요소를 줄입니다: 패널, 턴 넘기기, 킬피드, 배치 카드, 메뉴·시점 초기화 버튼. 창 크기가 바뀌면 다시 계산합니다.
  - 전에는 4:3과 5:4에서 흑·백 패널이 판 모서리의 알을 가렸습니다.
  - 배율(2인 패널): 16:9·21:9는 1, 16:10은 1(킬피드만 0.8), 4:3은 약 0.6, 5:4는 약 0.47입니다. 5:4에서는 글씨가 작습니다.
- 결과 창과 게임 중 메뉴는 HUD가 높이 1080을 확보하므로 모든 비율에서 들어갑니다. 설정 카드(1320 폭)는 HUD의 가장 좁은 캔버스(5:4, 1350)에도 들어갑니다.
- **조작 안내** (`ControlsHint`, 2026-09-26): 오른쪽 열의 메뉴 버튼 위(3~4인이면 턴 넘기기 위)에 튕기기, 조준 취소, 시점 키, 메뉴를 보여 줍니다. 키는 이 기기의 바인딩을 따라 바뀝니다. 판과도 여백을 두고 줄어들며, 0.75배보다 작아지는 4:3·5:4에서는 숨깁니다(16:10은 0.81배).
- **알림**(대전 시작, 시간 초과, 턴 넘김, 탈락)은 2.5초 동안 차례 표시 자리에 대신 뜹니다. 전에는 차례 표시 아래, 판 윗줄 위에 떠서 그 줄의 알을 가렸습니다.
- **배경**: 위에서 내려다보면 식탁보만 보이게 식탁(`tableCloth`)을 넓혔습니다(x 1.8배, z는 시점 이동 범위까지 덮도록 ±2.8, 가운데 정렬). 의자는 식탁에 겹치지 않게 조금 밀었습니다. 전에는 식탁보가 왼쪽으로 치우쳐 있었고, 양옆에 식탁 나무색 띠가, 아래에 바닥 자투리가 보였습니다. 21:9까지 식탁보만 보이고, 옆으로 시점을 옮기면 식탁 가장자리가 보입니다.
- 천장 선풍기(Animator로 3초에 한 바퀴)는 그림자를 끄지 않았을 때 천장 스포트라이트 그림자가 판 옆을 주기적으로 지나갔습니다. 선풍기 그림자만 껐고, 자유 시점에서는 그대로 돕니다.

## 4인 대전 (온라인, 2026-09-25)

- **인원**: 로비 규칙의 "인원"(최대 2/3/4명, 기본 2명)이 Steam 로비의 최대 인원입니다(`lobby.MaxMembers`). 호스트는 2명부터 시작할 수 있고, 그때 로비에 있는 사람이 모두 참가합니다. 이미 들어온 인원보다 낮출 수 없습니다. 로컬 대전 설정 카드에는 온라인 전용 규칙(`MatchSettingDef.OnlineOnly`)이 나오지 않습니다.
- **명단** (`MatchRoster`): 호스트가 0번, 나머지는 Steam 로비 멤버 순서입니다(`SteamLobbyManager.SeatOrder`, 로비 화면의 자리 순서와 같음). `LoadGameScene`에 규칙과 함께 실려 가서 모든 머신이 같은 진영을 만듭니다. 명단에 없는 사람(시작 순간에 들어온 사람)은 그 매치를 무시합니다. 로컬 시작은 명단을 지웁니다.
- **진영**
  - 바둑알: 흑·백·청·적. 청·적 돌은 흰 돌 재질에 색을 입혀 만듭니다.
  - 장기말: 초·한·청·흑. 청은 초의 글자(楚, 卒), 흑은 한의 글자(漢, 兵)를 씁니다. 한자 글꼴이 9자뿐이라 새 글자는 넣지 않았습니다.
  - 이름과 색은 `SideStyle`에 있습니다.
- **자리** (`BoardSetup.Seat`): 2인은 남·북(예전과 같음). 3~4인은 남쪽부터 시계 방향으로 남·서·북·동에 앉고(3인은 북쪽이 빕니다), 턴도 이 순서입니다.
  - 배치: 3~4인은 한 줄(5개까지) 또는 두 줄(최대 5+7)로, 대각선 쪽을 비워 이웃과 겹치지 않습니다(`MultiLayout`).
  - 배치 구역: 폭 1.4, 가운데에서 0.8 떨어진 곳부터 가장자리까지입니다.
  - 장기판(폭 2.8, 깊이 3.0)의 서·동은 판이 좁은 만큼 안쪽으로 옮겼습니다. 앞줄은 가운데 접힌 선(경첩)을 비웁니다(전에는 경첩 위 알이 5° 기울었음).
- **규칙**: 마지막까지 남은 한 진영이 이깁니다.
  - 알이 모두 나간 진영은 탈락, 기권한 진영은 기권, 나간 진영은 나감으로 처리되고(`TurnController.Forfeit`, `OnPlayerOut`), 턴을 건너뛰며 관전합니다. 그 진영의 알은 판에 남아 장애물이 됩니다.
  - 나머지가 한 진영만 남으면 그 진영이 이깁니다. 2인에서는 곧바로 상대 승리라 예전과 같습니다.
  - 동시 소진: 쏜 쪽 승리 / 무승부 / 쏜 쪽 패배. 쏜 쪽 패배는 그 샷 전에 남아 있던 다른 진영이 하나일 때만 그 진영이 이기고, 여럿이면 무승부입니다.
  - 점수("잡은 알"): 쏜 쪽이 남의 알을 떨어뜨리면 쏜 쪽 점수입니다. 자기 알은 2인이면 상대 점수(예전과 같음), 3~4인이면 아무 점수도 아닙니다(`ClassicRuleset.ScorerFor`).
- **네트워크**
  - 호스트는 명단의 게스트가 모두 `ClientReady`를 보내거나 나가야 시작하고, 로딩 중에 나간 게스트는 시작하자마자 나감 처리합니다.
  - 게스트의 튕기기는 **보낸 사람 자신의 알만** 받습니다.
  - 배치 상태는 게스트마다 따로 보냅니다(비공개 배치). 소리는 받는 사람의 튕기기만 빼고 보냅니다.
  - 탈락·기권·나감은 `PlayerOut`으로 알립니다.
  - 게스트 사이도 P2P로 연결되어 있지만, 호스트가 아닌 사람이 보낸 경기 메시지는 무시합니다.
  - 호스트가 나가면 3~4인은 "대전 종료"(승자 없음, `MatchEndReason.HostLeft`), 2인은 예전처럼 기권승입니다.
- **재대결**: 남아 있는 모두가 원하면 그 사람들로 다시 시작합니다(로비로 간 사람과 나간 사람은 빠짐, 2명 이상). 결과 화면에 "재대결 2/3명"이 표시됩니다.
- **HUD**
  - 2인은 그대로입니다.
  - 3~4인은 패널 네 개를 0.58배로 줄여 자기 변 가까이에 둡니다: 남·서는 왼쪽 열 아래부터, 북·동은 오른쪽 열 위부터입니다. 턴 넘기기 버튼은 메뉴 버튼 위로 옮깁니다(`MainGameUIController.Arrange`).
  - 패널 이름 줄은 온라인에서 Steam 이름이고, 빠진 진영은 흐리게 "탈락/기권/나감"으로 표시합니다.
  - 결과판은 4열이고, 사유는 "청만 끝까지 남았어요", "다른 쪽이 모두 기권하거나 나갔어요" 등입니다. 연속 전적은 "흑 1 · 백 0 · 청 2 · 적 0"처럼 나오고, 로비와 명단이 같을 때만 이어집니다.
  - 배치 카드는 인원만큼 시계 줄이 생깁니다.
- **로비**: 자리 4줄(방장 먼저). 방장에게 게스트마다 내보내기 버튼, 첫 빈자리에 초대 버튼이 있습니다. 상태 칩은 "3/4명 · 시작할 수 있어요", 공개 로비 목록은 "바둑판 · 바둑알 · 2/4명"으로 표시합니다.

## 완료된 작업

### 2026-07 (Windows)

1. **로컬 코어 재작성** (`9ae7891`): `TurnController` / `IRuleset` / `ClassicRuleset` / `PieceSelector`로 분리하고, 정지 감지를 속도 폴링 방식으로 바꿨습니다.
2. **Steam P2P 네트워킹** (`44b98c0`, `de476b0`): Facepunch.Steamworks 기반의 호스트 권위 구조입니다. 호스트만 물리를 시뮬레이션하고, 게스트는 kinematic + 보간으로 따라갑니다.
3. **Steam 출시 준비** (`27c10b6`): App ID를 `steam_appid.txt`에서 런타임에 읽고, bundle id를 정리했습니다.

### 2026-09-22 (Mac 이전 + UI 오버홀)

| 커밋 | 내용 |
|---|---|
| `f385e50` | Packages 추적 복구, ugui 추가, LobbyScene을 Build Settings에 등록 |
| `5cefea3` | Cinemachine 2.10.7 복원 (GameScene 누락 스크립트 9 → 0) |
| `3eb2882` | UI 킷: Pretendard 폰트, 생성 스프라이트, 한/영 다국어 (`Loc`) |
| `320ae1a` | 씬 3개를 Unity 6 포맷으로 재저장 (내용 변경 없음) |
| `91ce20c` | 인게임 HUD 재구성 + `GameManager.EndMatch` (두 번째 판부터 플레이어가 4명이 되던 버그) |
| `97d208f` | 메인 메뉴 재구성 (가짜 로그인/로비 허브, `SceneChanger` 제거), 부팅 시 Steam 초기화 |
| `500344e` | 로비 UI 재구성, Steam 친구 초대/수락, 로비 핸들러 누수 수정 |
| `27aa744` | `pieceID` 중복 수정 (온라인에서 돌 12개 중 2개만 동기화되던 문제) |

### 2026-09-23 (첫 2대 실전 테스트 후 수정)

첫 Mac(호스트)–Windows(게스트) 테스트에서 발견한 문제를 고쳤습니다: 백 턴이 끝나지 않음, 게스트 화면이 움직이지 않음, 중도 퇴장 무반응, 끝나면 멈춤.

| 커밋 | 내용 |
|---|---|
| `31f7a49` | 게스트 튕기기가 턴으로 처리되지 않던 문제, 턴 종료 확정 동기화, 게스트 이동 방식, 튕긴 직후 정지 판정 레이스, Steam 반쯤 초기화 상태 방어 (이전 세션 NRE 47,036회) |
| `5918abc` | 킷 폰트를 GUID를 유지한 채 다시 굽기 (다시 만들면 다른 prefab 폰트가 깨지던 문제) |
| `801aa95` | 게임 오버 화면(결과, 사유, 점수판, 경기 시간, 연속 전적), 온라인 재대결, 로비 복귀, 기권승, 동시 소진 규칙 |
| `80d9e34` | 조준 UI(링 게이지, 화살표, %, 첫 충돌 가이드), 가이드 설정(로컬 설정 / 로비 호스트) |
| `6125930` | 대전 규칙 체계(`MatchSettings`): 돌 개수(비대칭), 사전 배치/직접 배치(3가지 방식, 시간 초과 시 무작위), 턴 제한시간 + 스킵, 동시 소진 규칙 선택(무승부 포함), 게스트 로딩 전 매치 시작 방지 |

### 2026-09-24 (두 번째 실전 테스트 후)

| 커밋 | 내용 |
|---|---|
| `a6dee60` | 장기판(경첩 장애물) + 장기말, 설정 화면(언어·음량·키 바인딩), 시점 전환 키 Alt → Ctrl, 메인 메뉴 우측 상단 언어 토글 제거, 배치한 돌을 끌 때 로그 뷰어가 뜨던 문제, 구역 경계에 놓은 돌이 튕겨 나던 문제 |
| `a849f7f` | 킬로그(킬/논개/자살/팀킬) + 결과판 기록, 공개 로비(공개 범위, 목록, 빠른 대전, 프로토콜 버전, 대전 중 숨김, 내보내기), 장기말 한자 설정, 질량별 출발 속도(궁 약체 문제), 위로 뜨는 속도 제한(겹친 알 발사 문제), 경첩 높이 재검토(유지) |

| `cb6df94` | 제한시간 직전 샷이 시간 초과로 처리되어 게스트에 반영되지 않던 문제, 게스트 턴 0.5초 유예 |
| `14f0789` | 사운드(합성 12종, 호스트→게스트 소리 이벤트), 게임 중 메뉴(Esc, 기권), 로컬 AI 상대(쉬움/보통/어려움), 프로토콜 2 |
| `3591317` | 게임 속도 0.8배(시계는 실제 초), 마우스 가운데 버튼 시점 돌리기(커서 잠금), 만화풍 충돌 이펙트, 프로토콜 3 |
| `72dab32` | 가운데 버튼을 판 수평 이동으로 변경(범위 제한), 시점 초기화(R, HUD 버튼), 자유 시점이 옮긴 중심을 기준으로 돎, 카메라가 움직이는 동안 조준·배치 차단 |
| `350417d` | 온라인 최대 4인 대전(명단, 자리, 탈락·기권·나감, 다인 재대결, HUD·결과판·로비 4인), 프로토콜 4 |
| `c30a031` | 레이팅(Elo)과 Steam 리더보드 랭킹, 게임 속도 0.7배, 충돌 이펙트 세기별 크기·지속·페이드, 화면 비율 대응(메뉴·로비 Expand, HUD 여백 맞춤), 프로토콜 5 |
| `1bf42bc` | 모드 구분(일반전 = 레이팅 반영·밸런스 규칙 고정, 커스텀 = 모든 규칙·미반영, 아이템전 자리), 빠른 대전은 일반전만, 판·알 랜덤 |
| `2895703` | 마우스 휠 확대·축소, Windows 창 드래그 중 멈춤 방지, 충돌 이펙트 재설계(판 위·알 밑, 별 제거, 최대 0.4초) |
| `d5426b7` | 연출: 낙하(킬) 먹물 효과, 튕기기 고리, 턴 시작 고리, 집을 수 있는 알 표시, 효과음 6개(박, 알 놓기, 세기 눈금, 취소, 도장, 창 열기)와 로비 입장·퇴장 소리, 창 열림·버튼 반응·차례 표시·남은 알·결과 도장 움직임, 장면 전환 페이드, 대전 시작 알림 |
| `3c34aa4`, `9bda105` | 품질 개선: 화면 설정(전체 화면·창 크기, 처음 실행 전체 화면), 설정 카드 두 열, 조작 안내, 알림을 차례 표시 자리로, 결과판 빈 줄 제거, 배경 정리(식탁보, 선풍기 그림자), 릴리스 빌드에서 로그 뷰어 제거, 디버그 로그와 컴파일 경고 정리, 키 바인딩 캐시 |

세 번째 테스트 피드백: 배치 중 "예외"는 Reporter 창을 착각한 것이었고, 키 바인딩은 정상입니다.

## UI 시스템 (2026-09 오버홀)

- **움직임** (2026-09-26, 모두 `Time.unscaledTime`이라 일시정지 중에도 동작):
  - `PanelAppear`: 창(설정, 대전 설정, 랭킹, 게임 중 메뉴, 결과)이 열릴 때 어두운 배경이 페이드인하고 카드가 0.92배에서 살짝 넘치며 자리잡습니다(0.2초). 빌더의 `UIKit.Appear`가 붙입니다. 프레임이 길게 끊겨도 마지막 상태는 반드시 적용합니다(처음에 이걸 빠뜨려서, 열리는 순간 1.5초 멈추면 창이 투명한 채 남았습니다).
  - `ButtonFeel`: 모든 캡슐 버튼(`UIKit.CapsuleButton`)이 커서를 올리면 1.04배, 누르면 0.95배. 누르고 있는 동안만 크기를 건드리고, 그때의 크기를 기준으로 삼아 HUD가 여백에 맞춰 줄인 버튼도 그대로입니다. 비활성 버튼은 반응하지 않습니다.
  - `UIPulse`: 차례가 바뀔 때 차례 표시, 알을 잃을 때 패널의 남은 알 숫자, 알림이 뜰 때 한 번 튑니다. 결과 도장은 `slam`으로 2.4배·투명에서 내려앉고, 닿는 순간 도장 소리가 납니다.
  - `SceneFade`: 장면이 바뀔 때마다(첫 실행 포함) 메뉴 배경색에서 0.4초 동안 페이드인합니다. 코드로 만드는 오버레이라 씬·prefab에는 없습니다. 클릭은 막지 않습니다.
  - 킬피드 줄은 0.15초 동안 페이드인합니다.

스타일은 **한지(크림색 패널) + 먹(외곽선/텍스트) + 낙관 빨강(강조)**에 둥근 형태를 입혔습니다. 기준 해상도는 1920×1080이고, HUD는 높이 기준, 메뉴와 로비는 Expand(항상 1920×1080이 들어감)입니다. 아래 "화면 비율" 참고.

- **빌더**: `Tools > Alkkagi UI`
  1. `Generate kit assets`: 스프라이트, 폰트 에셋, TMP 기본 폰트
  2. `Build HUD` → `Assets/UI/MainGame_UI.prefab` + GameScene
  3. `Build main menu` → `Assets/UI/MainMenu_UI.prefab` + MainMenuScene
  4. `Build lobby` → `Assets/UI/Lobby_UI.prefab` + LobbyScene
- **빌더를 다시 돌리면 prefab을 덮어씁니다.** 인스펙터에서 손으로 다듬기 시작한 화면은 더 이상 빌더로 다시 만들지 말고, 필요한 변경은 빌더 코드(`Assets/Editor/AlkkagiUI/`)에 옮긴 뒤 재생성하세요.
- **UI 문자열을 추가하거나 바꾸면 반드시 `1. Generate kit assets`를 다시 실행하세요.** 폰트는 정적 SDF 아틀라스(`Loc` 테이블의 모든 글자 + ASCII)에 동적 fallback을 붙인 구조입니다. 새 글자를 굽지 않으면 fallback 아틀라스에 들어가고, 에디터에서 플레이할 때마다 .asset 파일이 바뀝니다. 같은 이유로 prefab의 자리표시자 텍스트도 테이블에 있는 글자나 ASCII만 쓰세요.
- 문자열 테이블은 `Assets/Scripts/UI/Loc.cs`에 있습니다. 한/영 두 언어이고, 선택은 PlayerPrefs에 저장되며, 기본값은 OS 언어입니다. 동적 텍스트를 쓰는 컨트롤러는 `Loc.OnLanguageChanged`를 구독합니다.
- HUD의 "남은 돌"은 `totalPieceCnt`가 아니라 **실제로 남아 있는 돌을 세서** 표시합니다. 게스트 쪽에서는 `totalPieceCnt`가 줄어들지 않기 때문입니다.
- 폰트: Pretendard 1.3.9 (SIL OFL 1.1, `Assets/Fonts/Pretendard/LICENSE.txt`). 상업 배포 가능합니다.
- 한자: Noto Serif KR Bold에서 장기 글자 9자(楚 漢 車 包 馬 象 士 卒 兵)만 fontTools로 잘라낸 4KB 파일입니다 (`Assets/Fonts/NotoSerifKR/`, SIL OFL 1.1). `1. Generate kit assets`가 256 크기 정적 아틀라스(`NotoSerifKR-Janggi SDF`)로 굽고, Pretendard 두 폰트의 첫 번째 fallback으로 붙입니다. 글자를 늘리려면 원본(github.com/notofonts/noto-cjk, `Serif/SubsetOTF/KR`)에서 다시 잘라야 합니다: `pyftsubset NotoSerifKR-Bold.otf --text="..." --layout-features='' --no-hinting --desubroutinize`.

## 설정 화면과 조작 (2026-09-24)

- **개인 설정**(이 기기에만 저장): 언어, 장기말 글자(한글/한자, `GameSettings.JanggiHanja`), 화면 모드와 창 크기, 전체 음량(`AudioListener`에 바로 적용), 인터페이스 음량(`GameSettings.InterfaceVolume`), 키 바인딩(`KeyBindings`)이 있습니다. 장기말 글자는 보이는 것만 바뀌고 두 사람이 서로 다르게 골라도 됩니다. "기본값으로"는 음량과 키만 되돌립니다. 인터페이스 음량은 클릭, 초읽기, 킬 같은 인터페이스 소리에 곱해집니다.
- 설정 카드는 두 열입니다(2026-09-26): 왼쪽 일반·화면, 오른쪽 소리·조작. 한 열로는 화면 항목을 넣으면 1080을 넘습니다.
- **창 드래그 (Windows)** (`WindowDragFix`, 2026-09-27): Windows는 제목 표시줄을 끄는 동안 창을 가진 스레드(Unity 메인 스레드)에서 자체 이동 루프를 돌려서, 창을 잡고 있는 동안 게임이 멈췄습니다(온라인이면 호스트 판 전체). 제목 표시줄 누름(`WM_NCLBUTTONDOWN`/`HTCAPTION`)을 가로채 그 루프가 시작되지 않게 하고, 버튼을 누르고 있는 동안 `Update`에서 창을 커서에 맞춰 옮깁니다(`SetWindowPos`).
  - Windows에 맡기는 것: 더블클릭 최대화, 최대화된 창 끌기, 가장자리로 크기 바꾸기(이건 여전히 멈춤), Alt+Space 이동. 이 기능이 켜져 있으면 화면 가장자리로 끌어 붙이기(스냅)는 되지 않습니다.
  - 문제가 생기면 실행 인수 `-nodragfix`로 끌 수 있습니다(Steam 속성의 실행 옵션).
  - Windows 빌드에만 컴파일됩니다(`UNITY_STANDALONE_WIN && !UNITY_EDITOR`). 이 Mac에서는 Unity의 Roslyn 컴파일러에 Windows 정의를 넣어 따로 컴파일해 보기만 했고, **Windows에서 실행해 보지는 못했습니다**.
  - Mac은 확인하지 못했습니다. Mac에서도 멈추면 원인이 달라 따로 봐야 합니다.
- **화면** (`DisplaySettings`, 2026-09-26): "전체 화면"은 디스플레이 해상도의 테두리 없는 전체 화면(`FullScreenWindow`)입니다. "창 모드"는 16:9 창 크기(1280×720부터 3840×2160까지 중 디스플레이보다 작은 것)를 ◀ ▶로 고릅니다. 창으로 바꾸면 디스플레이의 85% 안에 드는 가장 큰 크기로 시작합니다. 창을 손으로 늘려도 되고, 표시는 실제 크기를 따라갑니다. 저장은 Unity가 알아서 합니다(다음 실행 때 마지막 모드와 크기). 처음 실행은 전체 화면입니다(Player Settings의 Fullscreen Mode, 전에는 1280×720 창).
- 키 바인딩은 처음 읽을 때 PlayerPrefs에서 한 번 읽어 둡니다(`KeyBindings.Get`). 조준 취소 키는 알마다 매 프레임 확인해서, 전에는 프레임마다 PlayerPrefs를 수십 번 읽었습니다.
- 키 바인딩: 시점 전환(누르고 있기, 기본 왼쪽 Ctrl), 조준 취소(기본 마우스 오른쪽), 시점 이동(누르고 끌기, 기본 마우스 가운데), 시점 초기화(기본 R). 이미 쓰는 키를 고르면 두 동작의 키를 맞바꿉니다. 왼쪽 클릭과 Esc는 바인딩할 수 없습니다. 동작을 추가하려면 `GameAction`, `KeyBindings.Defaults`, `Loc`의 `bind.<동작>`을 넣고 메뉴를 다시 빌드하세요.
- **Reporter(Unity-Logs-Viewer)**: 2023 프로토타입부터 MainMenuScene에 있던 로그 뷰어입니다. 원래는 마우스 버튼을 누른 채 원을 한 바퀴 그리면 열렸는데, 돌을 끌거나 조준할 때도 열려서 제스처를 껐습니다(`numOfCircleToShow = 9999`). 대신 **에디터와 개발 빌드에서만 F12**로 엽니다(`ReporterHotkey`). 릴리스 빌드에서는 열 방법이 없으므로 시작하자마자 없앱니다(2026-09-26). 남겨 두면 모든 로그를 쌓고 매 프레임 FPS를 샘플링합니다. Reporter가 없어질 때 로그 훅을 풀도록 `Reporter.OnDestroy`에 한 줄 넣었습니다(원래는 풀지 않아, 없앤 뒤에도 스레드 로그가 쌓였을 것).
- macOS에서는 Ctrl+클릭이 오른쪽 클릭으로 처리되는 환경이 있습니다. 지금은 시점 전환(Ctrl) 중에 조준을 받지 않으므로 조준에는 영향이 없습니다.

## 아직 실제로 테스트하지 않은 것

2026-09-23 수정 사항은 아래 "네트워크 로직을 에디터 한 대로 테스트하기" 방식으로 메시지 단위까지 검증했습니다. 하지만 **실제 두 대에서는 다시 확인해야 합니다**:

- **공개 로비** (두 대 필요):
  - 한쪽이 공개 로비를 만들면 다른 쪽 목록에 뜨는지, 참가 버튼과 빠른 대전으로 들어가지는지
  - 친구 공개와 비공개 로비에 코드로 들어가지는지 (Steam 문서상 비공개는 초대 전용이라, 코드 참가가 막히면 선택지를 바꿔야 함)
  - 내보내기, 그리고 내보낸 사람이 다시 들어왔을 때 또 내보내지는지
  - 대전 중에는 목록에서 빠지는지
- 게스트 화면의 소리: 충돌, 경첩, 낙하가 화면과 맞는 때에 나는지(0.06초 지연이 적당한지), 자기 튕기기가 한 번만 나는지
- 게스트 화면의 충돌 이펙트가 소리와 같은 때와 곳에 나는지. 낙하 먹물과 튕기기 고리도 같은 경로입니다
- 2026-09-26 연출 (에디터에서는 효과·움직임을 프레임 캡처로, 소리는 파형·스펙트럼으로만 확인했습니다. **소리는 직접 들어 보지 못했습니다**):
  - 새 효과음 6개의 음색과 크기가 어울리는지, 세기 눈금 틱이 거슬리지 않는지(너무 잦으면 `GamePieceDragAndReleaseForce`의 10단계를 줄이세요)
  - 실제 마우스로 알 위에 커서를 올렸을 때 고리가 자연스러운지(에디터에서는 커서를 움직일 수 없어 조건만 확인)
  - 버튼 호버·누름 크기 변화, 창 열림 속도가 체감상 적당한지
- 실제 마우스로 시점 이동: 가운데 버튼으로 끌 때 판이 커서를 따라오는지, 가장자리에서 멈추는지, 이동 뒤 Ctrl 자유 시점이 옮긴 중심을 도는지. 에디터에서는 이동, 범위 제한, 초기화, 자유 시점 중심, 조준 차단을 코드로만 확인했습니다
- 조준 중에 Ctrl이나 가운데 버튼을 누르면 조준이 취소되는 것이 자연스러운지(특히 게스트)
- **4인 대전**: 에디터에서는 로컬 하네스(명단을 직접 넣은 4인 판)와 가짜 transport로 로직을 검증했습니다.
  - 확인한 것: 배치, 턴 건너뛰기, 탈락·기권·나감, 부정 튕기기 거부, 다인 재대결, 호스트 이탈
  - 실제로는 아직 확인하지 않은 것:
    - 3~4대가 한 로비에 들어가는지, 로비 자리 순서가 모든 머신에서 같은지(다르면 로비 화면의 색과 실제 진영이 어긋남)
    - 게스트끼리의 P2P 연결이 재대결 메시지를 전달하는지
    - 로비 멤버 수 제한(`MaxMembers`)이 인원 변경에 맞게 바뀌는지
    - 3~4인 비공개 배치에서 구역 모서리가 겹칠 때 호스트가 거절한 돌이 되돌아가는지
- 0.7배 속도의 체감(너무 느린지), 턴 제한시간의 체감
- **레이팅과 랭킹**: 에디터에서는 계산(2인·3인·4인, 동시 탈락, 호스트 이탈), 이탈 보류와 다음 실행 때 반영, 결과판 표시를 가짜 transport와 가짜 계정 번호로 확인했습니다. 랭킹 창의 Steam 조회도 확인했지만, 보드가 비어 있어 "기록 없음"만 보았습니다.
  - 실제로는 아직 확인하지 않은 것:
    - 첫 랭크전 뒤 리더보드에 점수가 올라가는지(`ReplaceScore`)
    - 친구가 아닌 사람의 이름이 표시되는지
    - 로비 멤버 데이터로 상대 레이팅이 칩에 뜨는지, 재대결 명단에 새 레이팅이 실리는지
    - 앱을 강제 종료한 뒤 다음 실행 때 패배가 반영되는지
- 4:3·5:4 모니터에서 실제로 줄어든 HUD가 읽기 좋은지, 창 크기를 바꾸는 중에 다시 배치되는지
- **Windows 창 드래그** (`WindowDragFix`): 창 모드에서 제목 표시줄을 끄는 동안 게임(특히 온라인 호스트)이 계속 움직이는지, 창이 커서를 매끄럽게 따라오는지, 여러 모니터 사이로 옮겨지는지, 더블클릭 최대화와 가장자리 크기 조정이 그대로인지. 이상하면 `-nodragfix`로 끄고 알려 주세요
- **휠 확대·축소**: 실제 휠과 트랙패드로 한 칸 변화량(5%)과 부드러움이 적당한지
- **새 충돌 이펙트**: 판 위에 그려진 먹 선이 바둑판 줄과 헷갈리지 않는지, 강한 충돌도 충분히 눈에 띄는지
- **화면 설정** (에디터에서는 `Screen.SetResolution`이 동작하지 않아 빌드에서만 확인 가능): 전체 화면 ↔ 창 전환, 창 크기 ◀ ▶, 처음 실행이 전체 화면인지, 다음 실행에 마지막 모드와 크기가 유지되는지, 게임 중 메뉴에서 바꿨을 때 HUD가 다시 배치되는지. Mac Retina에서 창 크기 숫자가 픽셀인지 포인트인지(`Screen.currentResolution` 기준)도 확인하세요
- 온라인 기권(호스트, 게스트 각각)과 게임 중 메뉴의 메인 메뉴로(상대 쪽에는 기권승)
- AI: 제한시간이 있는 규칙, 배치 규칙 3가지, 장기판
- 게스트 화면의 킬피드와 결과판 수치가 호스트와 같은지
- 제한시간 마지막 순간의 샷: 호스트 샷이 게스트 화면에 그대로 보이는지, 게스트가 0초 직전에 쏜 샷이 받아들여지는지
- 새 튕기기 힘(질량별 속도)과 위로 뜨는 속도 제한의 체감
- 장기판·장기말 온라인 대전: 양쪽에 같은 말(글자, 크기)이 생성되는지, 경첩을 넘는 샷이 게스트 화면에서도 자연스러운지
- 설정 창의 음량 슬라이더
- 로비 규칙 카드: 호스트가 바꾼 값이 게스트 카드에 바로 보이는지, 매치에 그대로 적용되는지 (비대칭 돌 개수로 확인하기 쉬움)
- 직접 배치 3가지 방식 전부. 특히 비공개 방식에서 게스트 화면에 호스트 돌이 끝날 때까지 안 보이는지, 게스트가 놓은 돌이 튀지 않는지, 시간 초과 시 무작위 배치
- 턴 제한시간의 게스트 쪽 카운트다운과 호스트 판정이 체감상 맞는지 (게스트는 자기 시계로 표시하고, 판정은 호스트가 합니다)
- 게스트의 턴 넘기기 버튼
- 게스트 턴 진행과 게스트 화면 동기화 (튕기는 동안의 스냅샷, 턴 종료 시 위치 확정)
- 중도 퇴장 (앱 종료, 네트워크 끊김) 시 상대 쪽 기권승 화면. 앱을 종료하면 로비를 떠나므로 바로 전달돼야 하고, 크래시는 Steam 타임아웃 뒤에 전달됩니다
- 온라인 재대결 핸드셰이크, 로비 복귀, 로비에서 새 판 시작
- **친구 초대**: Steam 오버레이가 필요해서 Steam으로 실행한 빌드에서만 동작합니다 (에디터 불가). `+connect_lobby`(게임이 꺼진 상태에서 초대 수락)도 미검증입니다.

## 네트워크 로직을 에디터 한 대로 테스트하기

`NetworkMatchBridge`는 `ISessionTransport` 인터페이스로 통신하므로, **보낸 메시지를 기록하는 가짜 transport를 주입하면** Steam이나 두 번째 머신 없이 호스트와 게스트 로직을 검증할 수 있습니다 (2026-09-23 수정은 이렇게 검증했습니다):

1. GameScene에서 Play → `MatchSettings.Current`에 원하는 규칙을 넣고 `GameManager.manager.gameState = GameReadyProcess`로 매치 시작 (규칙은 도메인 리로드 때 초기화되므로 Play 뒤에 넣으세요)
2. 로비가 없으니 `GamePreparation`은 로컬 경로로 바로 시작해 버립니다. 온라인 시작 경로를 시험하려면 되돌리세요: 배치 모드는 `Placement` 속성을 리플렉션으로 `null`로, 기본 배치는 시작하지 않은 새 `TurnController`로 교체하고, `gameState = WaitingForPlayers`로 둡니다
3. 빈 GameObject에 `NetworkMatchBridge` 추가. 로비가 없으니 `Start`에서 휴면 상태가 됩니다
4. 리플렉션으로 `transport`(가짜), `gameManager`, `isHost`, `hostId`, `opponentId`를 넣고 `BuildPieceLookup()` + `InitHost()`(또는 `InitGuest()`) 호출, `enabled = true`
5. `HandleMessage(senderId, NetMessage.Write...(...))`로 상대 메시지를 주입하고, 가짜 transport에 기록된 메시지와 게임 상태를 확인. 호스트는 `ClientReady`를 받아야 매치를 시작합니다
6. HUD는 매치 시작 시점에 브리지를 찾으므로, 나중에 붙인 브리지에는 `MainGameUIController`의 `networkBridge` 필드와 `SubscribeNetwork`로 수동 연결

레이팅이 걸린 매치를 시험할 때는 규칙에 `ApplyMode()`를 불러 일반전 규칙에 맞추세요(`new MatchSettings()`는 턴 제한 "없음"이라 미반영입니다). 그리고 먼저 `PlayerRating.Load(가짜 번호)`로 기록 주인을 바꾸세요. 리더보드에는 Steam에 로그인한 본인 번호의 기록만 올리므로, 가짜 번호의 결과는 올라가지 않습니다. 끝나면 `rank.<번호>.*` PlayerPrefs를 지우세요. 브리지의 `Start`는 활성화된 다음 프레임에 돌아 transport를 덮어쓰므로, 그 뒤에 한 번 더 넣어야 합니다. HUD의 레이팅 시작은 `MainGameUIController.BeginRating`(리플렉션)입니다. 이름 표시에 Steam이 필요하므로 MainMenuScene에서 시작하세요.

게스트 테스트는 `GameManager.SkipLocalTurnProcessing = true`로 두고 `StartMatch` → (배치 모드면 `PlacementState`) → 스냅샷 → `TurnResult` 순으로 주입하면 됩니다. 실제 턴을 돌리려면 `TurnController.TryApplyExternalFlick`로 튕기면 됩니다 (로컬 매치에서도 동작). 배치는 `GameManager.Placement.TryPlace/TryReady`로 직접 진행할 수 있습니다. 화면 반영(`PlacementController.LateUpdate`)은 다음 프레임에 일어납니다.

## 미결정/보류 사항

- 아이템전 콘텐츠: 코드 훅(`ITurnAction`, `MainGameUIController.ShowAvailableItems`)만 있고, UI 슬롯은 오버홀 때 제거했습니다.
- 조준 가이드는 경첩을 장애물로 치지 않습니다 (알과 판 가장자리만 봅니다).
- 킬 판정은 "그 샷을 쏜 사람"이 기준입니다. 어느 알이 직접 쳤는지(연쇄 킬 등)는 따지지 않습니다.
- 소리는 코드로 합성한 임시 소리입니다. 출시 전에는 녹음한 소리나 라이선스가 확실한 소리로 바꾸는 것을 권합니다.
- AI는 한 수 앞만 봅니다(상대의 다음 샷은 계산하지 않음). 아이템전이 생기면 아이템 사용도 AI에 가르쳐야 합니다.
- 뜨는 속도 제한 때문에 알이 다른 알을 뛰어넘는 일은 사실상 없어졌습니다. 점프를 게임 요소로 쓰려면 이 값을 다시 봐야 합니다.
- 사전 배치 좌표는 바둑판과 장기판이 같이 씁니다. 장기판 교차점에 맞추고 싶으면 판별 레이아웃이 필요합니다.
- **턴 넘기기 교착**: 양쪽이 계속 넘기면 게임이 끝나지 않습니다. 연속 스킵 제한이나 무승부 처리 같은 규칙이 필요할 수 있습니다 (미정). 일반전은 레이팅이 걸려 있어서, 교착이 나면 먼저 나가는 쪽이 지는 구조입니다.
- **출시 전 이름과 아이콘**: 제품 이름이 `DU_Alkkagi_Project`라서 창 제목과 앱 이름이 그렇게 보이고, 앱 아이콘이 없습니다. 제품 이름(과 회사 이름)을 바꾸면 **PlayerPrefs 저장 위치가 바뀌어** 레이팅 기록과 설정이 새로 시작됩니다(Steam 리더보드에서 판수가 더 많은 쪽을 받아 오므로 레이팅은 복구됨). 바꾸려면 테스트 인원이 적을 때 한 번에 바꾸는 편이 낫습니다.
- 체이스 카메라(`CM vcam_chasecam`의 `TargetGroup1`)는 예전에 씬에 있던 흑 돌을 따라갔는데, 그 돌이 지금은 비활성 템플릿입니다. 돌이 런타임에 생성되므로, 쓰려면 매치 시작 때 대상을 지정해야 합니다.
- 씬의 `Players_ref`(P1–P4)와 루트의 비활성 `GamePiece_GO`는 코드에서 쓰지 않는 예전 오브젝트입니다 (그대로 둠). 쓰지 않던 `SpawnPointPresets`는 새 `SpawnLayouts`로 대체했습니다.
- 튕기기 힘 조정: 기본값은 이전과 같습니다. 체감을 보고 `maxDragDistance`를 조정하세요.
- 로비에서 호스트가 나가면 Steam이 게스트에게 방장을 넘기고, 게스트는 혼자 남은 로비의 호스트가 됩니다 (다른 친구를 초대할 수 있고, 목록의 호스트 이름도 새 호스트로 바뀝니다). 그대로 둘지는 미정입니다.
- 호스트 권위 구조라서 공개 로비에서 모르는 사람이 호스트면 물리 판정을 조작할 수 있습니다. 레이팅도 각자 자기 값을 올리는 구조라 같은 한계가 있습니다(위 "레이팅과 랭킹"의 신뢰 문제).
- 레이팅 미정 사항:
  - 티어 이름(브론즈 등)을 둘지
  - 첫 몇 판은 K를 크게 할지(지금은 항상 32)
  - 일반전 고정 규칙의 범위(지금은 알 개수·배치·동시 소진 고정, 턴 제한 필수). 바꾸려면 `MatchSettings.Defs`의 `ranked:` 인자만 고치면 됩니다.
- 스크립팅 백엔드는 Mono입니다. **Apple Silicon macOS 빌드를 내려면 IL2CPP로 전환**해야 합니다.
- **지형·기믹 맵** (2026-09-25 검토만 함): 판은 `BoardVariant`(빌더로 만든 충돌체 묶음)이라 새 맵은 새 변형입니다.
  - **정적 지형은 싸게 됩니다**: 벽·돌담, 튕겨 내는 범퍼(탄성 재질), 구멍(안쪽 `DeathTrigger`), 빙판·모래(마찰 다른 재질 조각).
    - 네트워크는 규칙 하나(맵 id)만 더하면 되고, AI는 판 충돌체를 그대로 복제해 시뮬레이션하므로 따라갑니다.
    - 손볼 곳: 맵별 배치·구역(장애물 피하기), 조준 가이드가 벽을 보게 하기, 벽 충돌음·이펙트 분류, AI 후보 세기 추정이 직사각형·무장애물을 가정하는 부분
  - **턴 단위 기믹**(몇 턴마다 오르내리는 배리어, 줄어드는 판)은 턴 수로 정해지므로 양쪽이 계산만 맞추면 됩니다. 줄어드는 판은 3~4인 교착(서로 넘기기)도 풀어 줍니다.
  - **실시간 기믹**(회전 막대, 워프, 가속 패드)은 게스트 동기화(스냅샷에 기믹 상태)와 AI 모델링이 따로 필요해 가장 비쌉니다.
  - `maxRiseSpeed` 때문에 경사로 점프는 제한값을 맵별로 풀지 않으면 안 됩니다.
  - 4인용 맵은 4방향 대칭이어야 공정합니다.
- **출시 전 친구 테스트 배포** (2026-09-25 검토만 함):
  - 지금 빌드는 이미 Spacewar(480)로 돌아가므로 빌드 폴더를 압축해 보내면 됩니다. 친구는 Steam에 로그인한 채 exe를 실행하면 되고, 공개 로비 목록, 빠른 대전, 코드 참가, 친구 초대를 모두 쓸 수 있습니다.
  - 다만 Steam 상태에 "Spacewar"로 보이고, 480은 시험용이라 장기적인 배포 수단은 아닙니다.
  - 자체 App ID(등록비 $100)를 받으면 두 가지가 됩니다: 베타 키(Release State Override, 최대 약 2,500개, 첫 게임은 App ID 생성 3주 뒤부터 요청 가능), 또는 Steam Playtest(무료, 별도 앱, 신청·키·친구 초대 방식).
  - 이 Mac에는 Mac 빌드 모듈만 있습니다. Windows 빌드는 Windows 머신에서 하거나, Hub에서 Windows Build Support(Mono)를 설치해야 합니다.
  - **배포용 빌드 전에는 반드시 `unity-mcp-cli remove-plugin`** (아래 Unity-MCP 절).
- 로비 빈 좌석 테두리는 목업의 점선 대신 흐린 실선입니다 (9-slice에서는 점선이 늘어나기 때문).

## Unity-MCP (Claude ↔ Unity Editor 연동): 로컬 전용

IvanMurzak/Unity-MCP를 **이 Mac에만 설치**했고, 의도적으로 커밋하지 않았습니다.

- **transport는 stdio만 사용합니다.** `setup-mcp --transport http`를 쓰면 `https://ai-game.dev/...` **클라우드 릴레이**가 설정됩니다. ai-game.dev 계정은 등록하지 않았습니다.
- 플러그인은 처음 열 때 `connectionMode = Cloud`가 기본값입니다. `unity-mcp-cli bootstrap-local --url http://localhost:25033 --token <UserSettings/AI-Game-Developer-Config.json의 token> .`으로 Custom으로 바꾼 뒤, `Window → AI Game Developer`에서 Connect를 누르세요.
- **커밋하면 안 되는 흔적 6곳**: `Packages/manifest.json`·`packages-lock.json`의 MCP 항목, `ProjectSettings/PackageManagerSettings.asset`, `Assets/Plugins/NuGet/`, `.mcp.json`, `ProjectSettings/ProjectSettings.asset`의 `UNITY_MCP_*` scripting define. 이 파일들에 정당한 변경을 커밋해야 할 때는 해당 부분만 골라서 스테이징하세요.
- **릴리스 빌드 전에 반드시 `unity-mcp-cli remove-plugin <프로젝트 경로>`를 실행하세요.** `Assets/Plugins/NuGet/`의 DLL 42개(18MB)가 전 플랫폼 활성화 상태라 그대로 두면 빌드에 포함됩니다.
- 보안: 로컬 프로세스가 인증 없이 에디터에서 코드를 실행할 수 있는 구조입니다. 포트는 127.0.0.1에만 바인딩됩니다.

## Windows 머신 동기화 절차

1. `git pull`: 로컬의 (전에는 무시되던) `Packages/manifest.json`이 저장소 버전으로 덮어써집니다. Windows에서 MCP 설정을 했었다면 그 항목은 사라집니다.
2. 프로젝트 루트에 `steam_appid.txt` 생성 (`480`)
3. Unity로 열면 ugui, Cinemachine 2.10.7, TMP 폰트가 자동으로 들어옵니다. 컴파일 에러가 나면 매니페스트에서 빠진 패키지가 있다는 신호입니다.
4. (선택) MCP가 필요하면 Node.js를 설치한 뒤 위 "Unity-MCP" 절차를 그대로 따르세요. `UserSettings/`는 머신별이라 Cloud → Custom 설정도 다시 해야 합니다.
